# Flask Route Generator

Read GEMINI.md.

Generate Flask Blueprint routes.

Requirements:

* REST API
* Proper HTTP status codes
* JSON responses
* Use service layer only
* No business logic
* Add docstrings
* Handle exceptions correctly
* Return only the Python file.

